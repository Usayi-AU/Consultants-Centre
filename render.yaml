services:
  - type: web
    name: consultants-centre
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    preDeployCommand: python manage.py migrate && python manage.py seed_tracker_if_empty
    startCommand: gunicorn operations_dashboard.wsgi:application --log-file -
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: operations-dashboard-db
          property: connectionString

databases:
  - name: operations-dashboard-db
    databaseName: operations_dashboard
    user: operations_dashboard
